//array is denoted by []
var players = ['Steph Curry', 'Kevin Durant', 'Klay Thompson'];
console.log(players);

//Boolean literals
var happy = true;

//Floating point literal
4.1, 5.5, 10.1

// integers
// can appear in multiple forms: base 10 (0-9), Base 8 (octal; 0-7),
// Base 16 (hexidecimal; 0-e), or Base 2 (binary 0-1)

//object leterals  -  Similar to the object data-type

let x = { a:0, b:1, c:3 }
console.log(x.a);
console.log(x.b);
console.log(x.c);

//RegExp Literals

//  like this /ab+c/

//String literals

// must use singe ''  or double "" but cannot be used together
// cannot have 'hello', "hello" in a string

var a = ['money', 'cars', 'bush']
let b = { h:3, i:'junk', u:true}
console.log(a);
console.log(b.u);

ConvertBase.hex2dec = function (ee) {
    return ConvertBase(ee).from
}